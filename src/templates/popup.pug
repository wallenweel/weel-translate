doctype html
html(lang="")
  head
    meta(charset="utf-8")
    title Weel Translate Popup
    link(href="./css/popup.css" rel="stylesheet")
  body
    .container(data-actived-ui="")
      include inc/toolbar.pug

      main.content
        include inc/page-entry.pug
        
        .page.-preferences
          h1 Preferences
          
        .page.-settings
          .group.-translator
            h3 选择翻译引擎
            .item.-radio
              input(type="radio" name="api_src" value="google")
              label Google
            .item.-radio
              input(type="radio" name="api_src" value="youdao")
              label 有道
            .item.-radio
              input(type="radio" name="api_src" value="baidu")
              label 百度
              
          .group.-customAPI
            h3 应用自己的 API
            .item.-textarea
              textarea(name="custom_api")
            .info
              | 格式:
              code name id key;
              | 每条以 ";" 分隔

          .group.-interaction
            h3 选择交互方式

            .item.-checkbox
              label 使用浮动按钮
              input(type="checkbox" name="use_fab")
              i.-fake
            .item.-checkbox
              label 自动显示弹出面板
              input(type="checkbox" name="auto_popup")
              i.-fake
            .item.-checkbox
              label 浮动结果面板
              input(type="checkbox" name="use_fap")
              i.-fake
            
            .info
              | 全都不选的话，为手动打开

          .group
            h3 扩展管理
            button(type="button").uninstall.wave.-js 卸载扩展

        .page.-support
          h1 About

        .page.-feedback
          h1 Support

      section.drawer
        header.user-area
          .avatar
            img(alt="avatar" src="")
          .username user name
        
        nav.link-list
          a(href="entry.html").item.wave.-js
            i.i-home
            span __MSG_TRANSLATION__
          a(href="preferences.html").item.wave.-js
            i.i-style
            span __MSG_PREFERENCES__
          a(href="settings.html").item.wave.-js
            i.i-tune
            span __MSG_SETTINGS__
          a(href="support.html").item.wave.-js
            i.i-all_inclusive
            span __MSG_DONATE__
          a(href="feedback.html").item.wave.-js
            i.i-bug_report
            span __MSG_FEEDBACK__

      section.mask.-js

    script(src="js/popup.js")
